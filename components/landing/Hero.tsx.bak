'use client';

import Link from 'next/link';
import { LIST_MY_HOME_URL } from '@/lib/constants';

export default function Hero() {
  return (
    <section className="relative bg-[#3d5a5c] text-white overflow-hidden">
      {/* Background texture overlay */}
      <div className="absolute inset-0 opacity-10">
        <img src="/bg-texture.jpg" alt="" className="w-full h-full object-cover" />
      </div>

      <div className="relative max-w-7xl mx-auto px-6 py-16 md:py-24">
        <div className="grid md:grid-cols-2 gap-12 items-center">
          {/* Left Column: Headline & Content */}
          <div className="space-y-6 z-10">
            <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
              Keep the Equity on<br />
              One of the Biggest<br />
              Assets You Own.
            </h1>

            <p className="text-lg md:text-xl text-gray-200 leading-relaxed max-w-lg">
              You CAN sell your own home. The entire selling process, made simple! For a one-time cost of $795, get access to all the benefits of an agent without the percentage-based commission fees. Let us guide you through the home selling process.
            </p>

            {/* CTA Button */}
            <div className="pt-4">
              <a
                href={LIST_MY_HOME_URL}
                className="inline-block bg-yellow-400 text-gray-900 font-bold px-8 py-4 rounded-lg shadow-lg hover:bg-yellow-500 transition text-lg"
              >
                List Your Home Now
              </a>
            </div>
          </div>

          {/* Right Column: Hero Image with Badge */}
          <div className="hidden md:block relative z-10">
            <div className="relative rounded-xl overflow-hidden shadow-2xl">
              <img
                src="/hero-home.webp"
                alt="Beautiful home - SoldByYou"
                className="w-full h-auto"
              />

              {/* "We Home Sold or Your Money Back" Badge */}
              <div className="absolute top-4 right-4 w-32 h-32">
                <div className="relative w-full h-full bg-gradient-to-br from-teal-400 to-teal-600 rounded-full flex items-center justify-center shadow-xl border-4 border-white transform rotate-12">
                  <div className="text-center p-2 -rotate-12">
                    <div className="flex justify-center mb-1">
                      <span className="text-yellow-300 text-xl">★★★</span>
                    </div>
                    <div className="text-white font-bold text-xs leading-tight">
                      We Home Sold or<br />
                      Your Money Back
                    </div>
                    <div className="text-[10px] text-white mt-1 font-semibold">
                      Lifetime Guarantee
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
